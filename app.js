const API=(window.BITEFIXES_CONFIG?.API_BASE_URL||'').replace(/\/$/,'')+(window.BITEFIXES_CONFIG?.API_PREFIX||'');
const dialog=document.getElementById('biteyDialog');
document.getElementById('bitey').onclick=()=>dialog.showModal();
document.getElementById('close').onclick=()=>dialog.close();
document.querySelectorAll('.quick button').forEach(b=>b.onclick=()=>{document.getElementById('biteyInput').value=b.textContent+' — ';document.getElementById('biteyInput').focus()});
async function postJson(path,payload){const r=await fetch(API+path,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});if(!r.ok)throw new Error('API '+r.status);return r.json();}
document.getElementById('quoteForm').addEventListener('submit',async e=>{e.preventDefault();const f=new FormData(e.currentTarget);const status=document.getElementById('formStatus');status.textContent='Enviando solicitud...';try{const data=Object.fromEntries(f.entries());await postJson('/contact',data);status.textContent='Solicitud enviada. BiteFixes entrará en contacto contigo.';e.currentTarget.reset();}catch(err){status.textContent='No fue posible enviar ahora. La web de WordPress continúa disponible y puedes intentar nuevamente.';console.error(err);}});
document.getElementById('biteySend').onclick=async()=>{const input=document.getElementById('biteyInput');const text=input.value.trim();if(!text)return;const status=document.getElementById('biteyStatus');if(status)status.textContent='Bitey está pensando...';try{const data=await postJson('/chat',{message:text,source:'bitefixes-web',language:'pt-BR'});if(status)status.textContent=data.response||data.message||'Bitey recibió tu mensaje.';input.value='';}catch(err){if(status)status.textContent='Bitey no está disponible temporalmente. Intenta nuevamente.';console.error(err);}};
document.querySelector('.menu').onclick=()=>{const nav=document.querySelector('.site-header nav');nav.style.display=nav.style.display==='flex'?'none':'flex';nav.style.position='absolute';nav.style.top='72px';nav.style.left='0';nav.style.right='0';nav.style.padding='18px';nav.style.background='#fff';nav.style.flexDirection='column';};